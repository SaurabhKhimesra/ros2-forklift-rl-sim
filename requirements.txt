# Runtime dependencies for the RL side, and nothing else.
#
# ROS 2 packages belong in package.xml where rosdep can act on them, so they are
# not listed here. Figures and TensorBoard are optional: see requirements-viz.txt.

numpy>=1.23
gymnasium>=0.29
torch>=2.0
PyYAML>=6.0
